// Max image upload size — keep in sync with the backend
// (utils/files_helpers.MAX_IMAGE_UPLOAD_MB) and fleet-admin.
export const MAX_IMAGE_UPLOAD_MB = 25;
export const MAX_IMAGE_UPLOAD_BYTES = MAX_IMAGE_UPLOAD_MB * 1024 * 1024;
